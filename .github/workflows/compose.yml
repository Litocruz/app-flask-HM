name: Deploy con Docker Compose

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  deploy:
    #runs-on: self-hosted
    runs-on: ubuntu-latest


    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Debug secrets
      run: |
        echo "POSTGRES_USER is set: ${{ secrets.POSTGRES_USER != '' }}"
        echo "POSTGRES_PASSWORD is set: ${{ secrets.POSTGRES_PASSWORD != '' }}"
        echo "POSTGRES_DB is set: ${{ secrets.POSTGRES_DB != '' }}"
        echo "DB_HOST is set: ${{ secrets.DB_HOST != '' }}"
        echo "REDIS_HOST is set: ${{ secrets.REDIS_HOST != '' }}"

    - name: Crear archivo .env
      run: |
        echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" >> .env
        echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> .env
        echo "POSTGRES_DB=${{ secrets.POSTGRES_DB }}" >> .env
        echo "DB_HOST=${{ secrets.DB_HOST }}" >> .env
        echo "REDIS_HOST=${{ secrets.REDIS_HOST }}" >> .env

    - name: Parar servicios anteriores
      run: docker compose down -v || true

    - name: Construir servicios
      run: docker compose -f docker-compose.yml build

    - name: Levantar servicios
      run: docker compose -f docker-compose.yml up -d

    - name: Inicializar Base de Datos
      run: docker compose exec app flask init-db

    - name: Verificar app
      run: curl -f http://localhost/health